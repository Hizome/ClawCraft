# WarClaw Godot Scaffold

Quick start (Linux, Godot 4.6.1):
1. Open `project.godot` in Godot.
2. Run the project.

Notes:
- Default Gateway URL is `ws://127.0.0.1:18789` in `scenes/world/World.gd`.
- Update token/password in `autoload/GatewayClient.gd` as needed.
